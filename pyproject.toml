[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "epos-console"
version = "0.1.0"
description = "Local-first Sovereign Operating System"
authors = [{ name = "Exponere" }]
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
